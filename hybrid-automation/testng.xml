<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">

<suite name="Hybrid Framework Suite" parallel="false">

    <!-- LISTENER CONFIG -->
    <listeners>
        <listener class-name="listeners.TestListener"/>
    </listeners>

    <!--  TEST EXECUTION -->
    <test name="UI Tests">
        <classes>
            <class name="tests.SetupTest"/>
        </classes>
    </test>

</suite>